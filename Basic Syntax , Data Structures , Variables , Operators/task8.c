// 8. Find circumference of Rectangle formula : C = 4 * a

#include<stdio.h>
main()
{
	int length,a; // length and a 
	
	printf("Enter the length of rectangle is : "); // userinput length 
	scanf("%d",&length);
	
	a = 4 *(length); // formula a = 4*length
	
	printf("So The rectangle is : %d",a); // total 
}
